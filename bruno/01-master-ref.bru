meta {
  name: Master Ref
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}
  body: none
  auth: none
}

script:post-response {
  const master = res.body.refs.find(r => r.isMasterRef);
  if (master) {
    bru.setVar("masterRef", master.ref);
    bru.setEnvVar("masterRef", master.ref);
  }
}
