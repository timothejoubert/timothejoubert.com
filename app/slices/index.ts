// Barrel file expected by Slice Machine (see slicemachine.config.json `libraries`) and by
// SliceZone/SliceSimulator, mapping a slice's API ID to its Vue component. Populated automatically
// when a slice is created via `pnpm slicemachine` — currently empty, no slices exist yet.
export const components = {}
